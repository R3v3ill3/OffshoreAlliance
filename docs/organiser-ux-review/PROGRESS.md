# Progress ledger — Organiser UX plan

Orchestration of `docs/ORGANISER_UX_REVIEW_AND_PLAN.md` (7 September 2026). One row per work package. Decisions are in `DECISIONS.md`; per-package plans, verification output and reviewer findings are in `wp/<wp-id>.md`.

Status key: **not started** · **blocked (decision n)** · **planning** · **implementing** · **verifying** · **in review** · **PR draft** · **PR ready** · **merged** · **handed over** (operator runs it).

## Standing notes

- **Types:** the root `gen:types` script defaults to the production project; never run it without an
  explicit safe ref. **WP2.1 shipment exception (operator, 2026-09-13):** normal-dev schema/e2e is waived
  and types were generated from migrated clone `yqjkuobcawvigsfpgrcm` with
  `SUPABASE_PROJECT_REF=yqjkuobcawvigsfpgrcm pnpm gen:types` (exit 0). Other packages retain the normal-dev
  rule unless separately amended.
- **Migrations:** the ledger was rebased on 2026-09-08 into three baseline files under `supabase/migrations/`. Every migration file the appendices cite (`0002_rls_policies.sql`, `0013_campaign_workflow.sql`, `20260608100000_ou_group_integrity.sql`, …) now lives in `supabase/migrations_legacy/` and is audit-only. The live definitions are in `20260908050000_baseline_schema.sql`; implementers must read the baseline, not the legacy file, when changing a policy, trigger or view.
- **Plan documents:** the plan and appendices were authored on branch `claude/organiser-ux-campaign-workflow-bi0uai` and were not on `develop`. WP0.1 merges them into its branch so later packages can cite them.
- **Commands:** from `apps/organising-db`: `pnpm lint`, `pnpm test`, `pnpm build`. Root: `pnpm validate:migrations`.
- **Branches (from 2026-09-14):** `feat/oux-<wp-id>-<slug>` off `main`; draft PR into `main`; never push to `main` directly, pull requests only; never rebase, amend or force-push a pushed branch (merge `main` in instead); no worktrees; never commit `supabase/.temp/*`. **`develop` is parked** at `f5529a4a` (equal to `main` at the time of parking; it had been fast-forwarded after PR #43, so the phase-2 prompt's expected `68400084`, three commits behind, was already stale) and is not used: do not branch from it, merge into it, merge from it, or delete it. Ledger rows dated before 2026-09-14 reference `develop` as it was then. **Amendment 2026-09-15 (operator instruction):** small follow-up fixes may be committed straight to `main` (and `develop`, kept fast-forwarded to `main` on the same push) without a branch or pull request, after the orchestrator has verified them (typecheck, full unit suite, lint, own read of the diff); first such commit `67c3e9f8` (wall-chart sub-unit View inheritance). Work packages with a migration keep the branch + PR + promotion-gate flow. `develop` is no longer parked at `f5529a4a`; it equals `main`.
- **Promotion gate (from 2026-09-14, every package with a migration; generalises WP2.2's G1):** a merge to `main` deploys to Vercel Production immediately and regenerates `packages/db-types/generated.ts` from production, so a pull request that adds a migration is merged only after the operator has applied that migration to production. Order per package: migration file on the branch → applied to normal dev (operator, or the agent through the connector with the operator's approval for that exact file) → preview, e2e and contract tests green → operator applies the same file to production via a run sheet (one file per SQL Editor submission; `SET LOCAL oux.env = 'production';` after every `BEGIN;` in mutating files; read-only verification appended; `postgres` role; never `apply_migration` on production) → operator merges the pull request → any post-merge scripts.
- **Supabase projects (from 2026-09-14; unchanged for the rest of this development phase, do not propose changing it):** production `gteygwfgjvczanmrwgbr` (never read or written by an agent — **amended 2026-09-22 by the operator for the OA Universe alignment workstream, decision D0 in `docs/data-architecture/OA_UNIVERSE_ALIGNMENT_PLAN.md`: agents may run the read-only profiling pack under `scripts/data-hygiene/oa-universe/`, and equivalent SELECT-only, PII-free profiling queries, against production for that workstream; every write to production remains operator-run under the promotion gate**); normal dev `dpnnmkhabysfdogllsyh` (backs every Vercel Preview and the e2e accounts; thin: 8 units, 111 placements; has WP2.1; read freely, mutate with per-file approval); realistic data set `yqjkuobcawvigsfpgrcm` (`offshore-alliance-wp21-rehearsal`: production-shaped copy from 2026-09-12 with WP2.1 and cleanup applied, 22 campaigns, 2,407 workers, the 161-unit campaign; contains real worker contact details and real staff accounts; not wired to Vercel; read freely, mutate only under an approved run sheet; never wire it to Vercel, cron, webhooks or messaging credentials without the operator's explicit instruction). Its migration ledger is one behind the repo (`20260913000000_an_survey_reports`): apply that file there as part of the next approved run sheet that touches it. **Correction 2026-09-17 (read-only ledger check during WP3.8 planning):** `an_survey_reports` is present there; what the set lacks is WP2.2a `20260914090000` and WP2.2b `20260914090100`, which need WP2.2's cleanup sequence (`10` → `20` → `00` → `03b` → `04` → 2.2b) and are applied only under a run sheet of their own, not folded into another package's rehearsal. WP2.5's realistic-data performance measurement will need them; raise then. When a package needs realistic data (WP2.5 first; possibly WP2.4 drag rules and WP2.6), the planner says so and proposes the least invasive use, approved per package.
- **Realistic data set, from 2026-09-24 (OA Universe alignment D17):** the OA Universe workstream's fresh
  production-shaped clone is `plbldfctqhnbyrsypuri` (`OA_clone_2`; restored from a production backup after Phase 0 landed; marked
  `clone`; owner: the data-architecture workstream, see `docs/data-architecture/PROGRESS.md`). The 12 September set
  `yqjkuobcawvigsfpgrcm` is retired once that workstream records the pack comparison; WP2.5's realistic-data
  measurement should use the new ref and coordinate through the data-architecture ledger before mutating it.
- **PostgREST strings are proven against PostgREST (from 2026-09-22, WP3.8 hotfix #56):** a package that adds or changes a PostgREST embed, hint or `.or()` filter merges only after that string has run against a real PostgREST — the contract suite on dev, or a read-only REST probe on dev with the anon key (`curl …/rest/v1/<table>?select=<string>`; a parse error such as PGRST200 comes back even when RLS returns no rows). The jsdom harness's fake backend accepts any select string and proves nothing about it.
- **Lint baseline moved to 298 (146 errors / 152 warnings), measured on `main` at `e2cf34a2` on 2026-09-17.** It sat at
  294 through WP2.1–WP2.2 and 295 from the WP2.4 branch base. The last three problems arrived with the membership-refresh
  commits pushed to `main` by a separate session (`e83f7970`, `a93823d1`, `e23906ce`), not with any package of this
  programme: verified by measuring `pnpm lint` with and without the WP2.4c drag-auto-scroll files, which gives 298 both
  ways. Stages should compare against 298 until someone clears those three.
- **Baseline (develop at 1b959b1, measured 2026-09-08):** `pnpm build` green; `pnpm test` 632 passing, 1 test file fails to load (`src/lib/sms/__tests__/rating-source-taxonomy.test.ts` reads a migration the baseline rebase moved to `migrations_legacy/`; a stale path, fixed in WP0.2); `pnpm lint` 143 errors / 151 warnings, all pre-existing. Per-package standard until the debt is cleared: every touched file lints clean on its changed lines and the total error count must not rise. Clearing the debt is a housekeeping task for the operator to authorise, not part of any work package.
- **Local environment:** `apps/organising-db/.env.local` points at the **production** project. No agent may run the app locally (`pnpm dev`, `pnpm start`) or take screenshots until the operator supplies a dev-pointed environment; `pnpm build` alone is permitted (it does not query the database; every route is dynamic). Screenshot acceptance evidence is deferred until then and PRs stay draft.
- **Previews and e2e (2026-09-08):** every `feat/oux-*` branch gets a Vercel Preview deployment backed by dev; agents run `pnpm e2e` with `E2E_BASE_URL` set to the branch preview and the operator-supplied `E2E_USER_*` variables sourced from the shell profile (never printed, never stored in the repo). Flow one passed against the WP0.4 preview (tip of the phase-0 stack) on 2026-09-08 with the `user`-role account. Dev data change for that run, reversible: campaign 1 (`organiser_id` 4 → 10) so the e2e account owns one active campaign with 95 members and 4 units.

## Ledger

| WP | Title | Status | Branch | PR | Verification | Open risks | Decisions consumed |
|---|---|---|---|---|---|---|---|
| 0.1 | Decision register and branch setup | merged | `feat/oux-wp0.1-decision-register` | [#22](https://github.com/R3v3ill3/OffshoreAlliance/pull/22) | docs only | — | 1–10 recorded |
| 0.2 | Instrumentation and test harness | merged | `feat/oux-wp0.2-instrumentation-e2e` | [#25](https://github.com/R3v3ill3/OffshoreAlliance/pull/25) | lint at baseline (143/151); 732 tests pass (taxonomy test repaired and re-bounded); tsc, build green; `pnpm e2e` skips cleanly without credentials on cached Chromium 1194 | Flow one PASSED on the dev preview 2026-09-08 with the `user` account; events not observed in PostHog (no dev project, deferred) | — |
| 0.3 | Defaults, copy and layout quick wins | merged | `feat/oux-wp0.3-defaults-copy-layout` | [#24](https://github.com/R3v3ill3/OffshoreAlliance/pull/24) | lint at baseline (143/151), 657 tests pass + 1 pre-existing collection failure (WP0.2), build green; screenshots captured on the dev preview 2026-09-08 (wp0.3.md §7) | DataTable card view now fires on phones/iPads; needs visual check | — |
| 0.4 | Data hygiene scripts | merged; production phase-1 portion complete (2026-09-11) | `feat/oux-wp0.4-data-hygiene` | [#26](https://github.com/R3v3ill3/OffshoreAlliance/pull/26) | Dev rehearsals complete. Production: audit log secured; 02 inserted 21 roster rows (0 missing, 0 multiple-lead conflicts); 03 safely deleted 1 eligible duplicate; 01 converted 7 organisers after WP1.6 was live (final shape: 8 `user`/organisers, 4 admins), with sign-out/in and unit-delete spot-check green | 03 found 29 further excess placements in five partitions containing orphaned historical rule rows (`assignment_source='rule'`, `assigned_rule_id IS NULL`); deliberately deferred to a reviewed pre-WP2.1 data fix | 2, 8 |
| 0.5 | Usability baseline pack | merged | `feat/oux-wp0.5-usability-baseline-pack` | [#23](https://github.com/R3v3ill3/OffshoreAlliance/pull/23) | docs only: lint unchanged | — | — |
| 1.1 | Module registry and workspace mode | merged (2026-09-09) | `feat/oux-wp1.1-workspace-mode` | [#27](https://github.com/R3v3ill3/OffshoreAlliance/pull/27) | migration `20260909100000_workspace_mode.sql` applied to dev, types regenerated; 780 tests, tsc, build green, lint at baseline; role probe: `user` reads defaults, sees 0 app_settings rows | Two admin-editor advisories deferred; nothing consumes the hook yet (WP1.2/1.4) | 1 |
| 1.2 | Navigation driven by modules | merged (2026-09-09) | `feat/oux-wp1.2-nav-modules` | [#30](https://github.com/R3v3ill3/OffshoreAlliance/pull/30) | 884 tests incl. nine nav-model snapshots, a full-mode literal and a falsifiable reachability suite; full e2e suite green on preview incl. the organiser round trip; screenshots in evidence/wp1.2 | Full-mode sidebar flashes for organisers while the profile loads (WP1.1 fail-open; recorded) | 1 |
| 1.3 | My campaigns home | merged (2026-09-09) | `feat/oux-wp1.3-my-campaigns` | [#31](https://github.com/R3v3ill3/OffshoreAlliance/pull/31) | migration on dev, types regenerated; 936 tests; full e2e green in full mode; flow one under 10 s in organiser mode ×2 on preview; screenshots in evidence/wp1.3 | Profile-less signed-in users wait the 5 s failsafe; `/campaigns` stays reachable via "See all campaigns" | 1 |
| 1.4 | Campaign workspace | merged (2026-09-09) | `feat/oux-wp1.4-campaign-workspace` | [#32](https://github.com/R3v3ill3/OffshoreAlliance/pull/32) | 1006 tests incl. the 45-surface census and header-action map; full e2e suite green ×1 on the final preview incl. the organiser round trip; screenshots in evidence/wp1.4 | On the default module set only Role check is live in More (others muted, reachable by deep link or Show everything); full-mode users now see the campaign header above the four wizards (intended) | 1, 7 |
| 1.5 | Actions hub | merged (2026-09-09) | `feat/oux-wp1.5-actions-hub` | [#28](https://github.com/R3v3ill3/OffshoreAlliance/pull/28) | 828 tests, tsc, build green, lint at baseline; credentialled e2e on the branch preview: hub spec, `/sms` redirect and flow one pass | Sidebar still labels the hub "SMS Tools" until WP1.2; moderation count capped by PostgREST max-rows at very high relay volumes | 10 |
| 1.6 | Auth and RLS alignment | merged (2026-09-09) | `feat/oux-wp1.6-auth-rls` | [#29](https://github.com/R3v3ill3/OffshoreAlliance/pull/29) | Migrations `20260909120000` and `20260909130000` on dev; 41-line role probe pack (user, viewer, self-escalation, service role); full e2e both projects green ×2 on preview; 852 tests, tsc, build, lint at baseline | Behaviour changes signed off (unit writes only on own campaigns; no auto-enrol into unwritable campaigns); production pre-flight must return zero rows before deploy and again before hygiene 01; auth start-up deadlock fixed here (scope addition) | 2, 8 |
| 1.7 | Guides and hints | merged (2026-09-09) | `feat/oux-wp1.7-guides-hints` | [#33](https://github.com/R3v3ill3/OffshoreAlliance/pull/33) | migrations `20260911090000` (owner-only dismissals) and `20260911100000` (CHECK) on dev; 1029 tests; both hint specs pass on the final preview with product-scroll evidence; screenshots in evidence/wp1.7 | Intermittent: WP1.4's organiser-campaign spec can fail when the hint is visible and the New action menu is open (hidden accessibility tree); passes alone. Resolved by the tests-only follow-up 1.7f (seeded dismissal; each spec pins its own workspace mode) | 9 |
| 1.7f | E2E preconditions (follow-up, tests only) | merged (2026-09-10) | `feat/oux-wp1.7-e2e-hint-seed` | [#34](https://github.com/R3v3ill3/OffshoreAlliance/pull/34) | global setup seeds the `wall_chart_rating` dismissal for the e2e user; specs pin full/organiser mode per suite via the admin API and restore the recorded prefs; two consecutive green full runs on the develop preview, and a third on the branch preview after the reviewer advisories (loud seed failure, restore note on any changed document); credential-less run skips 14 | Reviewer: APPROVE WITH ADVISORIES, both applied | |
| 1.1f | Users dialog overflow (follow-up, operator-reported 2026-09-10) | merged (2026-09-10) | `fix/oux-wp1.1-users-dialog-scroll` | [#35](https://github.com/R3v3ill3/OffshoreAlliance/pull/35) | edit-user dialog capped at the viewport with a scrolling body and pinned Save; module checklist in two columns; "Default for role" option now shows the resolved mode; new admin spec proves Save is inside the viewport before and after scrolling, green on the branch preview with screenshot evidence | Reviewer: APPROVE WITH ADVISORIES (two-column checklist also reflows the Settings card; `title` tooltip is hover-only) | 1 |
| 2.1 | Schema and migration | **complete on production 2026-09-14.** Code merged 2026-09-12 UTC, on `main` via [#40](https://github.com/R3v3ill3/OffshoreAlliance/pull/40) at `82ff71c`; migration applied to production 2026-09-13; cleanup (C1 13 mappings / 16 flagged units; 03b 100 partitions / 177 placements removed / 1 operator override) and passing `04` on 2026-09-14 (`wp/wp2.1.md` §15); reviewer **APPROVE FOR PR/MAIN WITH PRODUCTION DB GATE** (gate now passed) | `feat/oux-wp2.1-schema-migration` | [#39](https://github.com/R3v3ill3/OffshoreAlliance/pull/39) merged at `de338b5` | Clone types exit 0; tsc exit 0; migration validation 10; F1 37/37; full tests 83/1,118; touched ESLint exit 0; lint baseline 294 (143/151); Next 16 build exit 0 / 130 routes. CI green on `de338b5`/`82ff71c`. Vercel Production success on `82ff71c`, `5fe7c93`, `9138424f`. **Production:** catalog check 20 groups / both `group_id` columns / ledger row; `04` passed with F1 residual 0, H10 0, 226 fallback targets suppressed (identical to the clone simulation), all `ou_dependant` and view checksums unchanged from the preflight. `main` regen `9138424f` keeps the WP2.1 symbols. | Migration was applied **before** the cleanup (run-sheet order inverted; `04` STOP at 134 caught it); recovered cleanup bytes ran on production without the clone re-rehearsal, so the two rollback scripts have never run on those bytes; 25P01 retained; `95` clone-only. Normal dev also has the migration (verified read-only 2026-09-14 via the Supabase connector: ledger row `20260912035329 wp2_1_campaign_groups` (and `20260913000000 an_survey_reports`) present; `campaign_groups` and `user_campaign_prefs` tables, both `group_id` columns, 4 groups, 8 units all grouped, 111 placements / 111 memberships, 0 flagged units, no membership view, no unique index); it needs no cleanup (F1/H10 residual 0). WP2.2 owns Insert typing, unpaged loader, API/enforcement/view/M2/Recompute before any group-model consumer | 3, 4, 5; E2/M2/C1/F1 |
| 2.2 | Structure API | **approved 2026-09-13** (R1, M2-a, K1, G1; §0 production/dev sequence); §0 step 1 (production WP2.1) **done 2026-09-14**; **Step 0 of the `main` orchestration done 2026-09-14** (`origin/main` `f5529a4a` merged in with `--no-ff` at `93df7e28`, docs-only, no conflicts; PR #41 retargeted to `main`; plan Revision 4: `main`-based §0/§6 sequence, sync-on-open as writer row 15a); **Stage 1 complete 2026-09-14** (migrations 2.2a `20260914090000` and 2.2b `20260914090100`, scripts `10`/`20`/`90`/`91`/`95` + README under `oux-wp2.2/`, typed wrapper + 51 unit tests, guard test, contract suite written for Stage 3; two static Fable reviews: CHANGES REQUIRED → fixed → APPROVE WITH ADVISORIES → applied; D1–D26 in `wp/wp2.2.md` §8.3; nothing has run on a database yet); **Complete on production 2026-09-15.** Post-merge checklist done: UI check → `10` (393 Employer placements, idempotent) → `20` (332 relabelled) → `00` → `03b` refused (one-shot guard; D82: the two new H9 rows listed read-only and removed by the operator by hand) → `04` passed → 2.2b applied and verified → read-only closure (manual=1440, universe=961, H9 0, view grants correct); GitHub deploy back on automatic. [#41](https://github.com/R3v3ill3/OffshoreAlliance/pull/41) at `cecdc380`, types regen `c1967d2a` kept the RPC symbols; post-merge checklist in progress: UI check → `10` → `20` → `00` → `03b` → `04` → 2.2b → check → GitHub deploy back on); **2.2a on production 2026-09-15** (operator run sheet; 17 RPCs / 14 helpers / three-value CHECK / anon refused / trigger relaxed / ledger row; H9 = 2 and 332 unattributed rows are live drift for `03b`/`20`; G1 step 2 met, PR #41 ready to merge; post-merge: `10` → `20` → `00` → `03b` → `04` → 2.2b → check); **Preview acceptance passed 2026-09-15** (operator ran §4.5 items 1–6 by hand on the branch preview: all six passed; the Playwright spec stays unrun — the sandbox runner cannot host a browser suite (D80/D81) and the operator declined GitHub secrets; **next: production 2.2a run sheet (G1 step 2), then merge**); **Stage 7 in progress 2026-09-14** (whole-PR Fable review APPROVE WITH ADVISORIES, A1/A2/A6 fixed at `c5820851`; types regenerated from dev `255c1b61` (D79); contract run 1 on dev 57 passed / 1 skipped (foreign-user gate); **2.2b applied to normal dev** through the connector under operator approval, verified read-only; contract run 2 and the preview e2e pending); **Stage 6 complete 2026-09-14** (lib and API-route writers rows 14–21 on the structure API: Recompute is one `replaceRuleRows` per campaign with rule attribution, the universe sync assigns `universe` rows with `skip` per unit and now targets the matched member's Employer container (§3.7), `loadOuTargets` and the `savePlacements` read paged; the guard test's acceptance case is green with an empty inventory; one Fable review round: APPROVE WITH ADVISORIES → A1–A6 applied; D60–D76; 1,368 tests; e2e still not run; `gen:types` from dev and 2.2b on dev pending); **Stage 5 complete 2026-09-14** (settings, wizard, units-section and hook writers rows 9–13 on the structure API via the shared `structure-save.ts`; wizard steps 5/6 gained visible errors; units re-saved in place and placement grids saved as a diff now preserve rows the legacy path lost; two Fable review rounds closed; D40–D59; T2 pending operator: unit type change in the edit dialog; 1,348 tests; e2e items 4–6 written, not yet run); **Stage 4 complete 2026-09-14** (wall-chart writers rows 1–8 + split/copy dialogs on the structure API; 1,280 tests; two Fable review rounds closed; D28–D39; nested-card double-move defect fixed; e2e items 1–3 written, not yet run); **Stage 2 complete** (2.2a on dev and rehearsed on the clone end to end incl. both rollbacks and the WP2.1 postflight; D27 reorders `04` before 2.2b); **Stage 3 pending** (contract suite could not reach dev from the credentialed environment — network allowlist; operator to set full access); **Stage 2 as first recorded:** 2.2a applied to normal dev 2026-09-14 (operator paste of the exact file; 32 function-body md5s verified; ledger row `20260914090000`; `95` probes passed and rolled back; PostgREST exposed schemas confirmed `public, graphql_public`); pending on dev: `01` marker, `10`, `20`; clone pass approved by the operator, run sheet to follow | `feat/oux-wp2.2-structure-api` (cut from `develop` at `1666660`, now carrying `main`) | [#41](https://github.com/R3v3ill3/OffshoreAlliance/pull/41) draft into `main` (plan only) | | §0 step 2 (WP2.1 on normal dev) found already done on 2026-09-14 (see WP2.1 row), so Stages 3–6 are unblocked on the database side. G1 still binds: PR #41 is not merged until production also has WP2.2a; the production sequence after the merge (`10` → `20` → `00` → `03b` if H9 > 0 → 2.2b → `04`) runs as one checklist with each output pasted before the next | E2/M2/C1/F1 handoff |
| 2.3 | Wall chart decomposition | merged | `feat/oux-wp2.3-wall-chart-decomposition` | [#38](https://github.com/R3v3ill3/OffshoreAlliance/pull/38) | Merged at `4d2ff4b`. Local: 83 files / 1093 tests, tsc/build green, lint unchanged at 294 and touched files clean; 2,635-line shell reduced to 320 lines; preview focused e2e 8/8, full e2e 15 passed / 2 established skips / 0 failed; terminal reviewer APPROVE WITH ADVISORIES | Fake PostgREST harness advisory retained. Existing nested-card drop bubbling invokes move twice and remains separately assigned | — |
| 2.4 | Group selector and per-group Unassigned | **merged 2026-09-15** ([#46](https://github.com/R3v3ill3/OffshoreAlliance/pull/46) at `5a16de07`; no migration; inert until the per-user `groups_v2` flag is ticked in Administration → Users). Stages 1–3 each reviewed (Fable, APPROVE, advisories applied; D1–D24); operator acceptance by hand on the preview passed (checklist steps 1–5); Stage 4 whole-package review **waived by the operator**; phase-2 metrics SQL (`scripts/data-hygiene/oux-wp2.4/00_phase2_metrics.sql`, read-only) **deferred to phase exit** (dev and production). **Open:** decision SM (cross-campaign mirror) → WP2.4b; 1,563 tests | `feat/oux-wp2.4-group-selector` | [#46](https://github.com/R3v3ill3/OffshoreAlliance/pull/46) merged at `5a16de07` | tsc clean; 1,563/1,562; lint 295 = base; guard 3/3; census 24/25 · 2 · 0 · 0; render cost v2 largest group ~2.8 s vs legacy ~8 s same run | Playwright groups-v2 spec written, unrun (D80/D81); WP2.8 removes the legacy chart and the flag | 2.1, 2.2, 2.3 |
| 2.4c | Nesting within a group (groups_v2 chart) | **Merged 2026-09-17** ([#49](https://github.com/R3v3ill3/OffshoreAlliance/pull/49) at `e2cf34a2`; the campaign-64 sector-campaign finding is in Incidental findings, 2026-09-17). Earlier: **approved 2026-09-15** (`wp/wp2.4c.md` Revision 1, all recommendations; no migration); **Stages 1–2 complete, Stage 3 pending the operator.** Stage 1 (pure library: `derive-group-tree.ts`, `plan-nested-drop.ts`, the resolver's `primaryIds`, the mutation's ordered `steps`, the campaign-42 fixture, `00_nesting_shape.sql`) and Stage 2 (the v2 chart renders the tree: nested cards inside the root, roll-up counts, SG-a primary groups, the nested planner on every drop, delete/merge/move dialogs, Units manager, SP-a) each reviewed by a fresh Fable with the blocking findings fixed (D1–D22; §11, §12). Stage 3 written here: `tests/e2e/groups-v2/nesting.spec.ts` + helpers (written and type-checked, **unrun** — D80/D81) and `wp/wp2.4c-acceptance-checklist.md` (HT-a, steps 0–14, the shape built through Split on dev). **Open: the operator's hand test on the branch preview, and the read-only `00_nesting_shape.sql` counts (dev and production campaign 42, which settle NP-b)** | `feat/oux-wp2.4c-nested-units` | | tsc exit 0; 1,658 tests passing (1 pre-existing legacy `wall-chart.render-cost` timing failure, untouched here); package suites 212/212 over 13 files (nesting 30, derive-group-tree 30, plan-nested-drop 23, resolve-group-selection 14, move-worker-mutation 8, guard 3); lint 295 = base (143/152) and every changed file clean; census 24/25 · 2 · 0 · 0 with the nested region; v2 nested render ~227 ms; `git diff main -- supabase/ packages/db-types/` empty | Playwright nesting spec written, unrun (no browser in the sandbox); no database of any kind touched in Stages 1–3 | 2.4 |
| 2.5 | Compare matrix | not started | | | | | |
| 2.6 | List view on shared state | not started | | | | | |
| 2.7 | Groups and units editor | not started | | | | | |
| 2.8 | Consumers, retirements and flag removal | not started | | | | | |
| 2.9 | Guides (series B and C) | not started | | | | | 9 |
| 3.1 | Three-screen create flow | not started | | | | | 6, 7 |
| 3.2 | Setup checklist drawer | not started | | | | | |
| 3.3 | Strategic plan module | not started | | | | | |
| 3.4 | Demote creation paths (decision 7) | not started | | | | | 7 |
| 3.5 | Action containers | not started | | | | | 10 |
| 3.6 | Link to campaign | not started | | | | | 10 |
| 3.7 | Guides (A4, A5) | not started | | | | | 9 |
| 3.8 | Campaign families and shared assessments (schema: `campaigns.parent_campaign_id`, `campaign_activities.scope`, `campaign_family_activity_ids()`, view rewrites, Basics "Part of", Shared-from section) | **Stage 1 complete 2026-09-17** (`f3888ef` + fix round 1: migration `20260917100000`, rollback `90`, data run sheet `10`/`91`, read-only `00`/`01`, `families.ts` + `campaign-parent.ts` with 34 unit tests, contract suite of 15 cases written; verifier green — validate 14, tsc 0, lint 298 = baseline, 1,746/1,747 tests with the known render-budget timing failure, build 0; fresh Fable static review APPROVE WITH ADVISORIES, A1–A4 applied (advisory-lock closure of the one-level trigger's concurrency hole, anon note, scoped run-sheet assertions), A5 carried to Stage 3). **Stage 2 database steps done 2026-09-21** under the operator's blanket approval ("all necessary steps up to pushing to main"): migration `20260917100000` on normal dev with its ledger row (whole-view checksum and counts identical); realistic-set rehearsal apply → rollback → re-apply with every checksum equal where predicted (`wp/wp3.8.md` §9.2 item 1), measurement 61: 2 rows / 62: 13 / 64: 0, view timing 27 ms for 64+57. **Stage 3 complete 2026-09-21** (`5fde140` + fix round 1 `1cbb64c`: every per-campaign assessment reader on the family rule incl. the 15 FQ-a sites and ten routes; Part of, Shared from, share toggle, selector groups, telemetry; +25 tests; verifier green — tsc 0, lint 298 = baseline, build 0, 1,771/1,772 with the known render-budget failure; fresh Fable review CHANGES REQUIRED (F1: Basics sheet sent `parent_campaign_id` on every save, so a child with an archived parent could not save) → fixed → APPROVE WITH ADVISORIES). **Operator chose the short path 2026-09-21:** production run sheet `scripts/data-hygiene/oux-wp3.8/prod/` (P1–P5, PX1/PX2) handed over; hand test on production after P4 instead of the preview; contract run when the `OUX_CONTRACT_*` accounts exist (CA). **Migration `20260917100000` on production 2026-09-21 (P2, operator; expected row; ledger row recorded); P3 checksums identical to P1; [#54](https://github.com/R3v3ill3/OffshoreAlliance/pull/54) merged 2026-09-21 at `72b259d8`** (operator asked the orchestrator to merge; `origin/main` merged into the branch first with no conflicts — eight commits incl. migrations `20260918120000` and `20260921030000` from separate sessions — and re-verified: 16 migrations validated, tsc 0, 1,819/1,820 tests, build 0). **P4 run on production 2026-09-21** (61, 62, 69 → part of 64; assessments 88–92 shared; 93, 95, 96 untouched). **P5 2026-09-21: ratings checksum unchanged, children 3, family 5 — production run sheet complete.** **Hand test on production 2026-09-21 FAILED at steps 1–3** (PostgREST PGRST200 on the self-referencing FK constraint-name hint; header fell back to the parent's id) → **hotfix [#56](https://github.com/R3v3ill3/OffshoreAlliance/pull/56) merged 2026-09-22 at `22cf94ab`** (column hint; wp3.8.md D39). **Hand test re-run 2026-09-22 after #56: shared section, Colour-by group and a rating recorded from Fugro visible in the sector campaign all PASS; parent's name still missing** (PostgREST resolves the column hint on a self-referencing FK in the children direction) → **hotfix 2 [#57](https://github.com/R3v3ill3/OffshoreAlliance/pull/57) merged 2026-09-22 at `c8bb3ee8`** (no embed: two primary-key reads; wp3.8.md D40). Open: operator confirms the parent's name in the header, the tab heading and the Basics sheet after the #57 deploy; contract run (CA) — **must run before the next merge that adds a PostgREST string**; §9.1 answered by the operator (all recommendations: FQ-a include, RD-a, FQ-c allow, FQ-d/e/f leave, FQ-g-a, TRG-a, CA, HT delete) — `wp/wp3.8.md` Revision 1 (Fable planner; 1,100 lines; reader inventory of 42 activity sites, 22 ratings sites and 19 database objects with a verdict each; finding F1: 28 further single-quoted readers the specification's grep missed). Nine §9.1 questions put to the operator (FQ-a…g, TRG-a, CA, HT), each with a recommendation. Decisions 11 and 12 confirmed by the operator the same day (SPN as SPN-a′); raised 2026-09-17 from the programme backlog; specification in `IMPLEMENTATION_ORCHESTRATION_PROMPT.md` phase 3; may start as soon as the decisions are answered, in parallel with WP2.5/2.6/2.7 (touches no structure file). Operator tasks 1–4 below precede planning. Reader inventory (17 app files, 8 database readers) recorded under decision 12 | `claude/serene-hamilton-8jy7hs` (the session's bound branch; the programme name `feat/oux-wp3.8-campaign-families` was not used) | [#54](https://github.com/R3v3ill3/OffshoreAlliance/pull/54) merged at `72b259d8` (2026-09-21, after P2/P3) | Stage 1–3 evidence in `wp/wp3.8.md` §9.2 items 1, 3, 4; contract run (item 2) pending CA credentials; hand test (item 5) on production after P4 | Migration; promotion gate applies; generated types carry `parent_campaign_id`/`scope` only after production; the campaign-64 data run sheet (parent of 61/62, sector-wide assessments → `family`) needs operator task 3 | 11, 12 |
| 3.9 | Start a campaign from this unit (spin-off: v2 card menu action + WP2.7 editor row → WP3.1 create flow prefilled; parent link, shared assessments, Worksite group built from Who's in) | **not started** — plan only after WP2.7 and WP3.1 are merged and WP3.8 is on production; implement after WP2.4b (MIR-a). Until then the TMT campaign is created by hand (operator task 1) and linked with WP3.8's Basics selector. Out of scope, recorded: SPN-b (moving sub-unit rows), merge-back, spinning off a sub-unit, multi-level families | `feat/oux-wp3.9-spin-off-unit` (proposed) | | | No migration expected (an idempotency key such as `spun_off_from_ou_id` would trigger the gate) | 11, 12; 6 |
| 4.1 | Touch and mobile | not started | | | | | |
| 4.2 | SOC-grain fields | not started | | | | | |
| 4.3 | Post-study and metrics | not started | | | | | |

## Phase exits

| Phase | Exit criteria | Evidence | Date |
|---|---|---|---|
| 0 | WP0.2–0.5 merged or handed over; baseline numbers recorded by the operator | PRs #22–#26 merged into develop 2026-09-08; flow one passed on the dev preview from a `user` account; WP0.3 screenshot-verified; WP0.4 rehearsed twice on dev and handed to the operator; usability baseline and PostHog visibility outstanding (human task / deferred) | 2026-09-08 (code complete; study pending) |
| 1 | Organiser mode on for the pilot group; e2e flow one green from a `user` account; no missing-feature report unanswerable via More or "Show everything" | **Complete.** [PR #37](https://github.com/R3v3ill3/OffshoreAlliance/pull/37) merged `develop` to `main` at `0f22d49`; Vercel Production succeeded; all six phase-1 migrations are present in the production ledger. Production roster backfill: 21 rows / 0 missing; WP1.6 preflight: 0 rows; seven organisers converted to `user` after policies were live. Pilot is all users with `work_role='organiser'`; the production role default is Organiser mode. Operator verified a converted organiser could sign out/in, open their campaigns, delete an empty unit, land on My campaigns with reduced navigation, and use Show everything. Existing preview evidence proves flow one and all 45 campaign surfaces remain reachable. | 2026-09-11 |
| 2 | e2e flows two and three green; share of memberships in a unit and median unit size reported from dev and production; no per-unit filter or view override remains | | |
| 3 | e2e flows four and five green; one creation path; a standalone action linked end to end on dev | | |

### Programme backlog (raised 2026-09-17, no package yet)

- **Spin off a unit into its own campaign, and share assessments across campaigns.** Raised by the operator from
  campaign 64: a sector campaign (job security / local employment) has one unit per company; when a company such as
  Total Marine Technology moves into bargaining it needs its own structure — vessels as a group, shifts as sub-units of
  each vessel — which is deeper than a sector campaign should carry. The ask is an explicit "spin off" of a unit into a
  campaign of its own. The harder half is assessments: those workers need **both** sector-wide assessments (a sector
  petition whose participation is counted across every employer) **and** campaign-specific ones (a protected action
  ballot that is only meaningful for that employer). Today an assessment belongs to one campaign, so this is a data-model
  question, not a UI one, and it interacts with the cross-campaign mirror (WP2.4b, decision SM) and with WP2.8's
  consumers. **To be planned by a fresh agent with its own package**, after the current phase-2 packages land; do not
  fold it into WP2.4c, WP2.5, WP2.6 or WP2.7.
  **Planned here (2026-09-17):** WP3.8 (families and shared assessments, schema) and WP3.9 (spin-off, UI), rows above;
  decisions 11 and 12 in `DECISIONS.md`; specifications appended to `IMPLEMENTATION_ORCHESTRATION_PROMPT.md` phase 3;
  plan §5.5 addendum records the sector-campaign shape as correct. SPN-b (moving the parent unit's sub-unit rows and
  placements into the child by RPC) is a recorded option, not planned.

## Human tasks (not code)

| Task | Raised by | Status |
|---|---|---|
| Run the usability baseline study (WP0.5 pack) | Phase 0 | pending |
| Run WP0.4 scripts on production (00 → 02 → 03; 01 held until WP1.6 is live) | WP0.4 | **Phase-1 portion complete 2026-09-11.** Production: 00 audit table present/secured; 02 inserted 21; 03 deleted 1 safe duplicate and deferred 29 excess orphan-rule placements to pre-WP2.1; 01 converted 7 after WP1.6 was live. Final role shape and organiser unit-delete flow operator-verified. **Dev run 2026-09-08:** 02 inserted 5, 03 deleted 2, 01 converted 4. |
| Apply WP2.1 to production (Track B: `00` → mappings → `03a` → `03b` → migration → `04`) | WP2.1 | **Complete 2026-09-14.** Migration applied 2026-09-13 (out of order, before cleanup); `04` STOP at 134 residuals; cleanup run 2026-09-14 in the SQL Editor from agent-prepared files: 13 canonical mappings, 16 units flagged, 100 partitions resolved (1 operator override: campaign 57 / worker 3014 → unit 556), 177 duplicate placements removed; `04` passed. Evidence `wp/wp2.1.md` §15. |
| Push the WP2.1 migration to normal dev (`wp/wp2.2.md` §0 step 2, one `supabase db push`, CLI linked to `dpnnmkhabysfdogllsyh`) | WP2.2 | **Already done** — found applied on 2026-09-14 (verified read-only 2026-09-14 via the Supabase connector: ledger row `20260912035329 wp2_1_campaign_groups` (and `20260913000000 an_survey_reports`) present; `campaign_groups` and `user_campaign_prefs` tables, both `group_id` columns, 4 groups, 8 units all grouped, 111 placements / 111 memberships, 0 flagged units, no membership view, no unique index). Who pushed it is not recorded; the ledger shows it landed together with `an_survey_reports`. |
| Switch the production project's Supabase GitHub integration deploy from **automatic on `main`** to off/manual before PR #41 is merged; switch it back only after the WP2.2 production sequence (2.2a → merge → `10` → `20` → `00` → `03b` if needed → `04` → 2.2b) is complete (`wp/wp2.2.md` §6.4 step 4a) | WP2.2 (operator report 2026-09-14) | **Done 2026-09-15**: switched to manual before the merge, back to automatic after step 8 (both migrations on production's ledger) |
| Add the four E2E_* repository secrets (E2E_USER_EMAIL, E2E_USER_PASSWORD, E2E_ADMIN_EMAIL, E2E_ADMIN_PASSWORD — dev accounts only) under GitHub → repository Settings → Secrets and variables → Actions, so `.github/workflows/e2e-preview.yml` (manually dispatched, branch previews only; wp2.2.md D81) can sign in | WP2.2 | **Declined by the operator 2026-09-15** — WP2.2 e2e acceptance is done by hand on the branch preview instead (wp/wp2.2.md §9.1); the workflow stays available for later packages if secrets are ever added |
| Re-record OVERVIEW clip (what changed on screen: campaigns open on the wall chart; My campaigns home and the four-tab organiser workspace in organiser mode; the first-use rating hint on first open; "Who's in" and "Unassigned" labels). Pipeline: docs/HOW_TO_VIDEOS_HANDOFF.md; note the publish step overwrites the manifest, whose route additions are pinned by a test | WP1.7 | pending |
| Re-record B1–B3, C1–C3 (C1/C2/B3 were candidates after WP0.3; deferred here by the orchestrator per plan §7) | WP2.9 | pending |
| Re-record A4, A5 | WP3.7 | pending |
| **Create "Total Marine Technology" as a bargaining campaign by hand** (the way campaigns 61 and 62 were): universe = employer 84 only (UNV-a), let the sync enrol members; Worksite group with one unit per vessel (Constructor, Normand); shifts under each vessel via Split with "keep in parent" on the legacy chart today, or WP2.7's Nest under once it lands (the campaign-42 shape WP2.4c renders). Do not mirror the vessels into 64. | WP3.8/3.9 orchestration, 2026-09-17 | **Done 2026-09-17** (operator: the Total Marine Technology campaign is **campaign 69** on production, with its two vessel units; the shift sub-units to be confirmed when the WP3.8 data run sheet is prepared). **Operator 2026-09-17: in the TMT campaign the vessels are to be Worksite units (roots of the Worksite group), not sub-units** — the spin-off shape the WP3.9 specification plans. Production placements on 64's units per the operator's query: 653 = 33 manual + 23 universe; 655 = 32 manual; 656 = 11 manual. Constraint found while answering: today only the wizard step 5 / settings units section write a `{worksite_id}` basis, and only for worksites in Who's in; the chart's New unit writes no worksite basis (`create-organising-unit-dialog.tsx:377`, `:411`), so a chart-made Worksite unit is a root card the sync never fills and a drag onto it never stamps the worker's worksite. Adding the vessels to Who's in under AND mode would shrink membership to workers whose record is on a vessel (5 of 33 on the 12 September data). Recorded for WP2.7 (§9.1) and WP3.9 as amendment **WSB**: a Worksite-group unit may be keyed to a worksite from the catalogue without that worksite being in Who's in, with both keys `{employer_id, worksite_id}` in a single-employer campaign. |
| **Keep campaign 64 employer-only**: no worksites added to its universe, match mode unchanged, nested vessel units not moved. If the two nested vessel units carry `unit_basis {worksite_id}` alone, set both keys (`{employer_id: 84, worksite_id: …}`) through the editor or a run sheet so the F1 matcher cannot place another employer's worker on them (`sync-campaign-universe.ts:156–162`). Read-only query for their current basis: `scripts/data-hygiene/oux-wp3.8/00_campaign64_vessel_unit_basis.sql` — the operator runs it on production and pastes the result (on the realistic data set of 12 September unit 653 has no children yet, so it returns the TMT row alone). | WP3.8/3.9 orchestration, 2026-09-17 | **Done 2026-09-17 — no change needed.** Operator's production result: 653 `{employer_id: 84}` (56 placements); 655 "constructor" and 656 "Normand" are `work_area` units in group 21 "Work area" with basis `{custom: true, dimension: 'work_area', parent_ou_id: 653}` (32 and 11 placements), no employer or worksite key, so the F1 matcher never targets them. Campaign 64 stays as it is. |
| **Decide which of campaign 64's seven assessments are sector-wide** so WP3.8's data run sheet can flag them `family`. **Applied on production 2026-09-21 (P4).** A later assessment 96 on campaign 64 is campaign-only; the operator can share it with the toggle if it is sector-wide. By title (realistic data set, 12 September; ratings in brackets): 88 "AN kickoff form" (43), 89 "Sector zoom interest" (30), 90 "Will ask others to join" (20), 91 "has visa story" (0), 92 "visa witness" (42), 93 "kiwi crew" (6), 95 "pia outreach aug 31" (12); all binary, organiser-assessed, supporter outcome "yes". The agent never decides this. | WP3.8/3.9 orchestration, 2026-09-17 | **Answered 2026-09-17 (operator):** 88, 89, 90, 91, 92 are sector-wide (→ `family` in the WP3.8 data run sheet); 93 "kiwi crew" and 95 "pia outreach aug 31" are company-specific (stay `campaign`) |
| **Answer decision SM (WP2.4b, `wp/wp2.4.md` §3.15)** if still open; recommendation SM-a stands and MIR-a makes WP2.4b a prerequisite of WP3.9. | WP3.8/3.9 orchestration, 2026-09-17 | pending |
| **Return to two Supabase projects (production and one dev) when this development process is finished** | Phase 2 `main` orchestration, 2026-09-14 | **End of programme, not before.** Likely shape: scrub the realistic data set `yqjkuobcawvigsfpgrcm` of contact details and staff accounts and make it the dev project, then pause and later delete the thin dev `dpnnmkhabysfdogllsyh`; that is decided then, not now. Until then the three projects stay as they are (production `gteygwfgjvczanmrwgbr`, never touched by an agent; thin dev behind every preview and the e2e accounts; realistic data set, not wired to Vercel, cron, webhooks or messaging). Nothing in phases 2–4 may make the return harder: keep the realistic data set's migration ledger in step with the repo (currently one behind, `20260913000000_an_survey_reports`, to be applied in the next approved run sheet that touches it) and keep every e2e fixture expressible through environment variables so it can be recreated in whichever project becomes dev. |

## Flag removals

_(dated notes when `workspace_mode` or the later-introduced `groups_v2` paths are deleted)_

## Incidental findings (not part of any work package until assigned)

| Found in | Finding | Assigned to |
|---|---|---|
| WP1.1 review (2026-09-09) | `user_profiles` "update own profile" policy plus table-wide grants let an authenticated `user` set their own `role`, `work_role`, `organiser_id` or `reports_to` via PostgREST; `get_user_role()` trusts the column. Exists in production today. | **Closed in WP1.6** (BEFORE UPDATE guard on role, work_role, organiser_id, reports_to, user_id; probe-proven) |
| WP1.4 planning (2026-09-09) | The SOC wizard is launched with `?cid=` from the campaign page and the phone wizard but reads only `campaign_id`, so it never pre-fills the campaign it was opened from. | Unassigned; small fix for WP1.4 or WP3.3 |
| WP1.1 verification (2026-09-09) | `supabase/.temp/` (the CLI link target) is tracked in git, so a fresh clone is linked to production. | Operator housekeeping: gitignore `supabase/.temp/` |
| WP0.3 review (2026-09-08) | Appendix D 3.2 counts 44 campaign-page surfaces; the SMS panel renders five views, so the inventory is 45. | Recorded in WP1.4's fixture |
| WP1.6 fix round 2 (2026-09-09) | Pre-existing auth start-up deadlock: the `onAuthStateChange` callback awaited a PostgREST query while auth-js held its initialisation lock, so warm full-page loads of small pages hung until "Hard Refresh Connection". Reproduced 8/8 on the preview before the fix. | **Closed in WP1.6** (callback body deferred per Supabase guidance; 6/6 warm loads pass after) |
| WP1.4 planning (2026-09-09) | Appendix D 3.2 counts 44 campaign-page surfaces; the SMS panel renders five views (Relays was missed), so the fixture carries 45. | Recorded in WP1.4's census |
| WP0.4 production run (2026-09-11) | Production has 193 historical rule-sourced placements with `assigned_rule_id IS NULL` across campaigns 42, 57 and 64. Five same-group/type worker partitions contain 29 excess placements; active rule definitions are only on campaigns 23 and 58, so the current 03 guard skips the remaining duplicates even though no live rule can recreate them. One eligible duplicate was safely removed. Cross-campaign membership is unaffected and remains supported. | **Closed 2026-09-14 by the WP2.1 production cleanup** (`03b`: 100 partitions, 177 placements removed; worker 3014 resolved by operator override to unit 556). |
| Operator, production use (2026-09-15) | Wall chart: a sub-unit ignores its parent unit's "View" (assessment view) selector — `effectiveAssessmentForScope` is a flat per-`ou_id` lookup with no parent walk, sub-unit cards render no "View" control, and depth-2 cards hard-code the "Cumulative" label (`wall-chart-model.ts:31`, `wall-chart-unit-hierarchy.tsx:596`). | **Fixed 2026-09-15 straight on `main` at the operator's instruction** (resolver walks up to the nearest ancestor override; nested labels resolved; regression tests) |
| WP3.8 production hand test (2026-09-21) | The parent-campaign loader embedded `campaigns!campaigns_parent_campaign_id_fkey(...)`; PostgREST returns PGRST200 for a constraint-name hint on a **self-referencing** FK (the column hint `campaigns!parent_campaign_id(...)` works). Reached production because (a) the jsdom harness's fake backend accepts any select string, (b) the contract suite that exercises PostgREST never ran (no `OUX_CONTRACT_*` accounts in the remote environment), and (c) the operator's short path replaced the preview hand test with a production one. Standing rule from here: **an embed or `.or()` string is proven only by a run against a real PostgREST (contract suite or a read-only REST probe on dev); a package that adds one does not merge without it.** | **Fixed by [#56](https://github.com/R3v3ill3/OffshoreAlliance/pull/56)** (2026-09-22); rule added to the standing notes |
| WP3.8 Stage 3 review (2026-09-21) | (a) `InlineRatingPopover` (tile rating in assessment view) emits no `family_assessment_rated` when its untouched callers hand it an `activityId` (wp3.8.md D25/D38); (b) no jsdom test asserts the three family events fire (the privacy test covers builders only); (c) after fix round 1's `isPending` switch, a *failed* parent load leaves the distribution charts and the participation-import assessment step in their loading state rather than an empty state (only reachable if the migration is absent or on a transient error; cheap fix `isPending && !parent.isError`). | Unassigned; for the next package that opens those files (WP2.8 consumers or WP3.9) |
| Operator, production query for WP3.8 task 2 (2026-09-17) | The two units nested under Total Marine Technology in campaign 64 are **custom Work area units** (655 "constructor", 656 "Normand"; `ou_type work_area`, group "Work area", basis `{custom: true, dimension: 'work_area', parent_ou_id: 653}`, 43 hand placements between them), not Worksite units keyed `{worksite_id}` as the 2026-09-17 campaign-64 finding above assumed. The sub-unit-only group hidden under SG-a there is therefore Work area, not Worksite; the correctness conclusion is unchanged. The vessel placements cannot be re-derived from worker records (no TMT worker has a `work_area_id` on the 12 September data set; 5 of 33 sit on a vessel-named worksite). | **WP3.9 planning (decision SPN):** a rebuild from Who's in cannot recreate hand-placed custom sub-units; SPN-a′ (recreate same-kind units in the child and assign the same members through the structure API) proposed to the operator in `DECISIONS.md`; plan §5.5 addendum and the WP2.4c checklist notes corrected in the same commit |
| Orchestrator, merge of `main` (2026-09-15) | Two commits by a separate "Cursor Agent" session landed on `main` in parallel (`e47b4d10`, `4461c2ee`): the universe sync now defaults campaign membership to employer AND worksite with a new match-mode control (`lib/workers/sync-campaign-universe.ts` +113 lines, `campaign-settings.tsx`, `campaign-universe-section.tsx`, `campaign-wizard.tsx`, `step-employers-worksites.tsx`, `universe-match-mode-control.tsx`), plus `scripts/data-hygiene/campaign-42-employer-mismatch/` (a read-only listing, a mutating `10_remove_wrong_employer_members.sql` and its rollback) outside the run-sheet process of this programme. On the merged `main`: guard test green (no direct structure writes), sync tests 53/53, `tsc` clean. | **Noted for WP2.4**: the plan's `path:line` citations into `sync-campaign-universe.ts` and the settings/universe files predate these commits — the implementer re-cites at Stage 1; the operator should say whether the Cursor session continues on those files so WP2.4 can avoid collisions. Any production run of the campaign-42 scripts is the operator's, outside this ledger |
| Operator, live testing of WP2.4 on production (2026-09-15) | With `groups_v2` on, sub-unit nesting is gone: campaign 42 has EDI Downer as employer, worksites as the primary grouping, and shift sub-units under the largest worksite; the v2 chart offers Shift as a sibling group, so every small-worksite worker appears as "Unassigned in Shift" beside the large worksite's not-yet-on-a-shift workers. Decision 5 ("no nesting") was wrong for this common shape. | **WP2.4c** (planned 2026-09-15): nested child units inside the parent card within the parent's group, parent area = members not yet in a child, sub-unit-only groups not offered as primary; WP2.5/WP2.6 plans to be re-checked against it before approval |
| Operator, campaign 64 context (2026-09-17) | Campaign 64 is a **sector** campaign: the primary group is Employers (one unit per company in the ROV sector), and the two nested units are work **vessels** ("constructor", "Normand") under Total Marine Technology, a company that has moved into bargaining. So a sector campaign legitimately nests Worksite-group units under Employer-group units, and the Worksite group there is sub-unit-only. Under SG-a it therefore leaves that campaign's Group selector — **correct for this shape**, not the surprise stop condition 6 was watching for: a sector view is organised by company, and Employers × Vessels stays available as WP2.5 Compare (CG-a). Only campaigns 42 and 64 have any nesting at all (00_/01_ on production 2026-09-16/17); every one of the 70 sub-unit placements is paired with its parent row, so no repair script is needed (NP-b closed). | **WP2.4c merges as planned**; `02_primary_groups.sql` kept in the repo but not needed to decide. Watch: a campaign where *some* worksites are nested and others are not keeps Worksite primary and shows the nested ones as flat "Parent › Unit" cards (A-2) |
| WP2.3 final review (2026-09-11) | Dropping on a nested sub-unit card bubbles to its parent and can call `moveWorkers.mutate` twice; the current no-op guard usually masks the second call. Characterisation pins the existing behaviour so WP2.3 does not change it. | **Fixed in WP2.2 Stage 4** (wp2.2.md §8.3 D32): the structure API made the second call user-visible (a drag from Unassigned onto a nested card landed the worker on the parent container), so the parent card now ignores a drop a nested card already consumed; the WP2.3 characterisation pins the single call |
| WP2.1 handoff (2026-09-13) | Generated `campaign_worker_ou.Insert.group_id` is required even though the database trigger derives it; the current universe OU loader is unpaged; Recompute can remove existing universe-sync rows. | **WP2.2:** resolve typing through the structure RPC/API boundary, page/uncap the loader, and fix/review Recompute before enforcement or any later group-model consumption |
| WP2.1 promotion (2026-09-13) | `.github/workflows/gen-types.yml` regenerates `packages/db-types/generated.ts` from **production** on every `main` push touching `supabase/migrations/**`. While production lacks the WP2.1 schema, each promotion strips the WP2.1 symbols from `main` (`5fe7c93`) and `develop` must re-merge `main` with `--no-ff` keeping its own `generated.ts` (`332331d`, `1666660`). Harmless at runtime today (untyped browser client; no consumer), but it will recur for every migration-carrying promotion, including WP2.2's. | **Closed 2026-09-14 for WP2.1 symbols**: production has the schema, and regen `9138424f` kept them. The same hazard recurs for WP2.2a's RPCs until they reach production, hence G1; RPC contract types stay hand-written |
| WP2.1 production run (2026-09-13) | The migration was applied to production before the cleanup steps, inverting the run sheet. Nothing broke because WP2.1 has no unique index, and `04`'s F1 gate caught it, but the order matters once WP2.2b exists. | Process note for WP2.2 §0 step 6: the operator run sheet must be executed as one checklist with each step's output pasted before the next |
| WP2.1 production run (2026-09-14) | Opening a campaign's wall chart runs a universe sync: `WorkforceBoard` POSTs `/api/campaigns/[id]/sync-universe-workers` on mount for any writer (5-minute `staleTime`). On production this enrolled 334 workers and inserted 233 placements in campaign 57 during the cleanup window. Not a defect of the cleanup (its checks passed), but membership/placement totals on production drift on page open, so before/after evidence must compare hazard counts, not row totals. It is also the writer that will hit WP2.2b's unique index first. | **WP2.2** (writer row 15a in `wp/wp2.2.md` §2.3 and §3.11: routed through the structure API by way of row 15, timing unchanged, guard test asserts no direct write in the route) and **WP2.4** (decides whether sync-on-open survives `groups_v2`) |
