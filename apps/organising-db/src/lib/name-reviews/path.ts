/**
 * Route of the Name Reviews page (DA0.3): the queue of employer and worksite
 * strings an import could not resolve to an existing row. Both import
 * wizards link here and the admin nav entry points here; one constant so the
 * two never drift (the `lib/membership-updates/href.ts` pattern).
 */
export const NAME_REVIEWS_PATH = "/name-reviews";
